# What the deployed Streamlit app (app/streamlit_app.py) actually imports — kept minimal
# on purpose. Streamlit Community Cloud installs exactly this file; a previous version of
# this file also included jupyter/ipykernel (needed only for running the notebooks locally,
# never imported by the app), which is a plausible reason a heavier, more failure-prone
# dependency tree could break a Streamlit Cloud build. See requirements-dev.txt for the
# full local-development set (notebooks, training script, tests).
#
# Exact versions verified to work together (retrained + tested against these, and the app
# was run end-to-end against this exact set — not just import-checked). If you need looser
# constraints for another platform, `pip install -r requirements.txt --upgrade` and re-test.
pandas==2.3.3
numpy==2.2.6
matplotlib==3.10.9
scikit-learn==1.7.2
xgboost==2.1.4  # pinned below latest: xgboost>=3.x's base_score serialization
                # (["5E-1"]-style string) breaks shap.TreeExplainer as of shap 0.49.1 —
                # confirmed by actually building the explainer, not assumed
shap==0.49.1
streamlit==1.63.0
joblib==1.6.0
