# Exact versions verified to work together (retrained + tested against these). If you need
# looser constraints for another platform, `pip install -r requirements.txt --upgrade` and re-test.
pandas==2.3.3
numpy==2.2.6
matplotlib==3.10.9
seaborn==0.13.2
scikit-learn==1.7.2
xgboost==2.1.4  # pinned below latest: xgboost>=3.x's base_score serialization
                # (["5E-1"]-style string) breaks shap.TreeExplainer as of shap 0.49.1 —
                # confirmed by actually building the explainer, not assumed
shap==0.49.1
imbalanced-learn==0.14.2
streamlit==1.63.0
joblib==1.6.0
jupyter==1.1.1
ipykernel==7.3.0
